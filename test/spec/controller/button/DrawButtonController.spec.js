describe('CpsiMapview.controller.button.DrawButtonController', function () {
    describe('Basics', function () {
        it('is defined', function () {
            expect(CpsiMapview.controller.button.DrawButtonController).not.to.be(undefined);
        });

        it('can be created', function () {
            var ctrl = new CpsiMapview.controller.button.DrawButtonController();
            expect(ctrl).to.not.be(undefined);
        });

    });
});
