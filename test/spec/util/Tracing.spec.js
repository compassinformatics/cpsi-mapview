describe('CpsiMapview.util.Tracing', function() {
    var cmp = CpsiMapview.util.Tracing;

    describe('Basics', function() {
        it('is defined', function() {
            expect(cmp).not.to.be(undefined);
        });
    });
});
